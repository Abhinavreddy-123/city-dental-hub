# Temporarily restore the admin footer link

## Change
- Add an **Admin Login** link to the public footer’s Help section.
- Point it to `/auth`, so staff can open the existing login page.
- Keep the direct `/auth` page unchanged.

## Later
- After the website is published, remove only this footer link and publish the update. Staff can still access `/auth` directly.
